; #388 refusal leg: naming hype's OWN boot/log medium must never arm.
[hype]
config_version = 1
log_level = debug

[vm.u]
vcpus = 1
mem_mb = 1024
boot = installer
install_media = \iso\test.iso
firmware = uefi
os_hint = linux
net_mode = none
target_disk = physical:HYPE388BOOT
